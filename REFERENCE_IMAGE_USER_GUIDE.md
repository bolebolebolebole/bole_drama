# 参考图功能使用指南

## ✅ 功能已实现！

参考图功能**已经完整实现**，包括：
1. ✅ **+ 按钮**上传自定义图片
2. ✅ **禁用功能**（悬停显示 X 图标）
3. ✅ **恢复功能**（双击禁用的图片）

## 📍 功能位置

### 位置 1：图片生成对话框（GenerateImageDialog）

**访问路径**：
1. 进入任意短剧项目
2. 点击"图片生成"或相关按钮
3. 在弹出的对话框中找到"参考图片"部分

**文件位置**：
- `src/web/src/views/generation/components/GenerateImageDialog.vue`
- 第 59-64 行

### 位置 2：专业编辑器 - 镜头图片生成（StoryboardEditor）

**访问路径**：
1. 进入短剧项目
2. 进入专业编辑器
3. 选择一个镜头
4. 切换到"场景制作"标签页
5. 找到"参考图片"部分

**文件位置**：
- `src/web/src/components/editor/StoryboardEditor.vue`
- 已添加 ReferenceImageManager 组件

## 🎯 如何使用参考图功能

### 步骤 1：添加参考图

#### 方法 A：点击 + 按钮
1. 找到"参考图片"部分
2. 点击右上角的蓝色 **"+ 添加图片"** 按钮
3. 在弹出的对话框中：
   - 拖拽图片到上传区域
   - 或点击上传区域选择文件
4. 点击"确定"按钮

#### 方法 B：空状态时添加
1. 如果还没有参考图，会显示空状态
2. 点击中间的 **"添加第一张图片"** 按钮
3. 按照上述步骤上传

**支持的格式**：
- JPG / JPEG
- PNG
- 最大文件大小：10MB
- 最多可添加：5 张参考图

### 步骤 2：管理参考图

#### 禁用参考图
1. **鼠标悬停**到参考图上
2. 右上角会出现**红色圆形 X 图标**
3. **单击 X 图标**
4. 图片变为半透明，显示"已禁用"遮罩
5. 图片下方显示"双击恢复"提示

**效果**：
- 禁用的图片不会发送到 AI 生成服务
- 图片仍然保留在列表中，可以随时恢复

#### 恢复参考图
1. 找到已禁用的图片（半透明 + 深色遮罩）
2. **双击图片**
3. 遮罩消失，图片恢复正常
4. 恢复后的图片会参与 AI 生成

### 步骤 3：生成图片

1. 填写其他必填字段（提示词等）
2. 点击"生成图片"按钮
3. 系统会使用：
   - 你的提示词
   - **所有启用的参考图**（禁用的不会使用）
4. AI 会根据参考图的风格和内容生成图片

## 🎨 UI 说明

### 正常状态
```
┌─────────────────────────────────────┐
│  参考图片                    [+ 添加图片] │
├─────────────────────────────────────┤
│  ┌────┐  ┌────┐  ┌────┐           │
│  │图1 │  │图2 │  │图3 │           │
│  └────┘  └────┘  └────┘           │
│  参考图1  参考图2  参考图3          │
└─────────────────────────────────────┘
```

### 悬停状态（显示 X 图标）
```
┌────┐
│ [X]│  ← 红色圆形 X 图标（右上角）
│图1 │
└────┘
参考图1
```

### 禁用状态
```
┌────┐
│▓▓▓▓│  ← 深色遮罩 + "已禁用"文字
│已禁│
│用  │
└────┘
双击恢复  ← 提示文字
```

## 🔍 故障排查

### 问题 1：看不到"参考图片"部分

**可能原因**：
1. 页面没有刷新
2. 使用的是旧版本的前端代码

**解决方案**：
```bash
# 1. 重新构建前端
cd src/web
npm run build

# 2. 刷新浏览器（Ctrl + F5 强制刷新）
```

### 问题 2：点击 + 按钮没有反应

**可能原因**：
1. 已达到最大数量限制（5 张）
2. JavaScript 错误

**解决方案**：
1. 检查是否已有 5 张参考图
2. 打开浏览器控制台（F12）查看错误
3. 如果有错误，刷新页面重试

### 问题 3：上传的图片没有显示

**可能原因**：
1. 图片格式不支持
2. 文件太大（超过 10MB）
3. 浏览器兼容性问题

**解决方案**：
1. 确认图片格式为 JPG 或 PNG
2. 检查文件大小是否小于 10MB
3. 尝试使用其他浏览器（Chrome 推荐）

### 问题 4：禁用的图片无法恢复

**可能原因**：
1. 双击速度太慢
2. 点击位置不对

**解决方案**：
1. 快速双击图片（不是文字）
2. 确保双击在图片区域内
3. 如果还是不行，刷新页面重试

### 问题 5：生成的图片没有参考图的风格

**可能原因**：
1. 参考图被禁用了
2. AI 模型不支持参考图
3. 参考图权重较低

**解决方案**：
1. 检查参考图是否启用（没有遮罩）
2. 确认使用的是支持参考图的模型（火山引擎 SeedDream 4.5）
3. 尝试使用更明显特征的参考图

## 📊 技术细节

### 数据流
```
用户上传图片
  ↓
创建 Blob URL (URL.createObjectURL)
  ↓
添加到 referenceImages 数组
  ↓
用户禁用/恢复（修改 deleted 标志）
  ↓
过滤出启用的图片
  ↓
发送到后端 API (reference_images 参数)
  ↓
后端传递给 AI 服务
  ↓
AI 生成图片
```

### API 参数
```typescript
{
  drama_id: "1",
  prompt: "一只可爱的猫咪",
  reference_images: [
    "blob:http://localhost:5678/xxx-xxx-xxx",
    "blob:http://localhost:5678/yyy-yyy-yyy"
  ],
  // ... 其他参数
}
```

### 组件接口
```typescript
// ReferenceImageManager.vue
interface Props {
  modelValue: string[]  // 参考图 URL 数组
  maxImages?: number    // 最大数量，默认 5
}

// 事件
emit('update:modelValue', activeImageUrls)  // 只包含启用的图片
```

## 🎯 最佳实践

### 1. 选择合适的参考图
- ✅ 风格统一的图片
- ✅ 清晰、高质量的图片
- ✅ 与目标内容相关的图片
- ❌ 避免模糊、低质量的图片
- ❌ 避免风格差异太大的图片

### 2. 参考图数量
- **1-2 张**：风格参考，效果最明显
- **3-5 张**：多样化参考，AI 会综合考虑
- **建议**：从 1-2 张开始，逐步增加

### 3. 禁用 vs 删除
- **禁用**：临时不使用，可以快速恢复
- **删除**：目前没有删除功能，只能禁用
- **建议**：使用禁用功能测试不同组合

### 4. 提示词配合
- 参考图 + 详细提示词 = 最佳效果
- 提示词描述你想要的内容
- 参考图提供风格和氛围

## 📝 示例场景

### 场景 1：生成角色立绘
```
提示词：一位年轻女性，长发，现代服装，微笑
参考图：
  - 图1：喜欢的画风示例（启用）
  - 图2：服装参考（启用）
  - 图3：不喜欢的风格（禁用）
```

### 场景 2：生成场景背景
```
提示词：室内，客厅，现代装修，明亮光线
参考图：
  - 图1：室内布局参考（启用）
  - 图2：色调参考（启用）
  - 图3：家具风格（启用）
```

### 场景 3：测试不同风格
```
第一次生成：
  - 图1：写实风格（启用）
  - 图2：动漫风格（禁用）
  
第二次生成：
  - 图1：写实风格（禁用）
  - 图2：动漫风格（启用）
  
对比效果，选择最佳风格
```

## 🚀 快速开始

### 5 分钟快速测试

1. **打开图片生成对话框**
   - 进入任意短剧项目
   - 点击"图片生成"

2. **添加参考图**
   - 点击"+ 添加图片"
   - 上传 1-2 张图片

3. **填写提示词**
   - 输入你想生成的内容描述

4. **生成图片**
   - 点击"生成图片"按钮
   - 等待 10-30 秒

5. **查看结果**
   - 对比有无参考图的效果差异

## 📞 技术支持

如果遇到问题：
1. 查看浏览器控制台（F12）
2. 检查网络请求（Network 标签）
3. 参考本文档的故障排查部分
4. 确认前端已重新构建

---

**功能状态**：✅ 已完整实现
**最后更新**：2026-01-23
**版本**：v1.0
