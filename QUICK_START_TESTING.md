# Quick Start - Testing Reference Image Manager

## 快速测试指南

### 启动应用

#### 方式 1: 开发模式（推荐用于测试）

```bash
# 终端 1: 启动后端
cd E:\AI\Bole_drama\src
go run main.go

# 终端 2: 启动前端开发服务器
cd E:\AI\Bole_drama\src\web
npm run dev
```

访问: `http://localhost:3012`

#### 方式 2: 生产构建模式

```bash
# 1. 构建前端
cd E:\AI\Bole_drama\src\web
npm run build

# 2. 启动后端（会自动服务前端静态文件）
cd E:\AI\Bole_drama\src
go run main.go
```

访问: `http://localhost:5678`

### 测试步骤

#### 1. 导航到参考图管理
1. 打开浏览器访问应用
2. 创建或选择一个短剧项目
3. 进入图片生成对话框
4. 找到"参考图片"部分

#### 2. 测试添加图片
```
操作: 点击"添加图片"按钮
预期: 
  ✓ 上传对话框打开
  ✓ 可以拖拽或点击上传
  ✓ 上传成功后图片显示在网格中
  ✓ 提示"图片添加成功"
```

#### 3. 测试悬停显示 X 图标
```
操作: 鼠标移到图片上
预期:
  ✓ X 图标在右上角淡入显示
  ✓ 图片边框变为蓝色
  ✓ 鼠标移开后 X 图标淡出
```

#### 4. 测试禁用图片
```
操作: 悬停后点击 X 图标
预期:
  ✓ 图片变为半透明
  ✓ 显示深色遮罩层
  ✓ 遮罩层中间显示"已禁用"
  ✓ 图片下方显示"双击恢复"
  ✓ 提示"图片已禁用"
```

#### 5. 测试恢复图片
```
操作: 在禁用的图片上双击
预期:
  ✓ 遮罩层消失
  ✓ 图片恢复正常透明度
  ✓ "双击恢复"提示消失
  ✓ 悬停时 X 图标重新出现
  ✓ 提示"图片已恢复"
```

#### 6. 测试多图片管理
```
操作: 添加 3 张图片，禁用第 2 张
预期:
  ✓ 第 1 和第 3 张图片正常工作
  ✓ 第 2 张图片显示禁用状态
  ✓ 可以独立恢复第 2 张图片
```

#### 7. 测试图片生成
```
操作: 
  1. 添加 3 张参考图
  2. 禁用 1 张
  3. 填写其他必填字段
  4. 点击"生成图片"
  
预期:
  ✓ 只有 2 张活跃图片发送到 API
  ✓ 禁用的图片不包含在请求中
  ✓ 生成任务成功提交
```

### 检查清单

#### 视觉检查
- [ ] X 图标位置正确（右上角 4px）
- [ ] X 图标大小合适（24px 圆形）
- [ ] X 图标颜色正确（红色背景，白色图标）
- [ ] 悬停动画流畅（淡入淡出）
- [ ] 禁用遮罩层清晰可见
- [ ] "已禁用"文字居中显示
- [ ] "双击恢复"提示清晰

#### 功能检查
- [ ] 添加图片功能正常
- [ ] X 图标只在悬停时显示
- [ ] 单击 X 禁用图片
- [ ] 双击禁用图片恢复
- [ ] 禁用图片不发送到 API
- [ ] 恢复图片发送到 API
- [ ] 最大数量限制生效（5 张）

#### 交互检查
- [ ] 鼠标悬停有视觉反馈
- [ ] 点击 X 有即时反馈
- [ ] 双击响应灵敏
- [ ] 所有操作都有提示消息
- [ ] 边框颜色变化流畅

### 常见问题排查

#### 问题 1: X 图标不显示
```
检查:
  1. 鼠标是否真的悬停在图片上
  2. 图片是否处于禁用状态（禁用时不显示 X）
  3. 浏览器开发者工具检查 CSS
```

#### 问题 2: 双击不响应
```
检查:
  1. 图片是否处于禁用状态（只有禁用图片才响应双击）
  2. 双击速度是否太慢（需要在系统双击间隔内）
  3. 浏览器控制台是否有错误
```

#### 问题 3: 图片没有发送到 API
```
检查:
  1. 打开浏览器开发者工具 → Network 标签
  2. 点击"生成图片"
  3. 查看请求 payload 中的 reference_images 字段
  4. 确认只包含活跃（非禁用）图片
```

### 浏览器开发者工具

#### 检查网络请求
```
1. 按 F12 打开开发者工具
2. 切换到 Network 标签
3. 点击"生成图片"
4. 找到 /api/v1/images/generate 请求
5. 查看 Request Payload:
   {
     "reference_images": ["url1", "url2"],  // 只包含活跃图片
     ...
   }
```

#### 检查控制台错误
```
1. 按 F12 打开开发者工具
2. 切换到 Console 标签
3. 执行操作
4. 查看是否有红色错误信息
```

#### 检查元素样式
```
1. 按 F12 打开开发者工具
2. 点击左上角的选择元素工具
3. 点击 X 图标
4. 查看 Styles 面板中的 CSS
5. 确认 opacity 和其他样式正确
```

### 性能检查

#### 检查动画性能
```
1. 打开开发者工具 → Performance 标签
2. 点击 Record
3. 悬停图片多次
4. 停止录制
5. 查看是否有掉帧（应该保持 60fps）
```

### 截图对比

建议截图保存以下状态：
1. 正常状态（无悬停）
2. 悬停状态（X 图标显示）
3. 禁用状态（遮罩层显示）
4. 多图片混合状态（部分禁用）

### 测试报告模板

```markdown
## 测试报告

**测试日期**: YYYY-MM-DD
**测试人员**: [姓名]
**浏览器**: Chrome/Firefox/Safari [版本]
**操作系统**: Windows/macOS/Linux

### 测试结果

| 测试项 | 状态 | 备注 |
|--------|------|------|
| 添加图片 | ✅/❌ | |
| 悬停显示 X | ✅/❌ | |
| 单击禁用 | ✅/❌ | |
| 双击恢复 | ✅/❌ | |
| 多图片管理 | ✅/❌ | |
| API 集成 | ✅/❌ | |

### 发现的问题

1. [问题描述]
   - 重现步骤: ...
   - 预期结果: ...
   - 实际结果: ...
   - 截图: ...

### 建议

1. [改进建议]
```

### 快速验证命令

```bash
# 检查前端构建
cd E:\AI\Bole_drama\src\web
npm run build

# 检查后端编译
cd E:\AI\Bole_drama\src
go build -o test.exe .

# 清理测试文件
rm test.exe
```

### 联系支持

如果遇到问题：
1. 查看 `REFERENCE_IMAGE_TEST_PLAN.md` 详细测试计划
2. 查看 `REFERENCE_IMAGE_IMPLEMENTATION.md` 实现细节
3. 查看 `REFERENCE_IMAGE_VISUAL_CHANGES.md` 视觉变更说明
4. 联系开发团队

---

**祝测试顺利！** 🎉
